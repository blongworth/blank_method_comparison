---
title: "DIC Blanks"
author: "Brett Longworth"
date: "3/12/2020"
output: html_document
---

What is the effect of using WS process blanks for correcting water secondaries? 

1. Extract DIC data by wheel.
2. Find WS blanks by wheel and average
3. Apply LBC using WS blanks
4. Compare with DIC corrected with HY blanks

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

```{r}
library(amstools)
library(tidyverse)
library(here)
library(knitr)
library(odbc)

options(digits = 5)
theme_set(theme_light())

std <- read_csv(here("data/std.csv"))

filter(std, grepl("Caswell", name))
wstd <- std %>%
        filter(process == "WS",
               rec_num %in% c(1082, 17185, 83028, 53804, 2138, 140548, 36168,
                            55101, 39246, 32490, 32491,
                            32492, 36947, 148820, 159579)) # need fort caswell, maybe not in std?

wstd %>% 
        group_by(name, system) %>%
        summarize(Fm = mean(norm_ratio))

ggplot(wstd, aes(norm_ratio)) +
  geom_histogram() +
  facet_grid(system ~ rec_num, scales = "free")
```

